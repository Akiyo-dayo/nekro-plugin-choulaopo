# 更新日志

## 1.1.1（2026-08-21）

同时兼容原版 NekroAgent 和 akiyo 版 NekroAgent。

- 命令输出类型 `CommandOutputSegment` 先从公开 `api.plugin` 导入，原版未再导出时回退到 `services.command.schemas`，避免在官方镜像上加载失败。

## 1.1.0（2026-08-20）

把抽老婆从「抽一次看笑话」扩成可互相折腾的群戏。

- 新增 `抢老婆 @某人`：抢对方当正缘。无人认领则强娶；已有主则按稀有度/契合度拼概率；`保护老婆` 中必失败。成功会挤掉你原来的正缘（喜新厌旧）。
- 新增 `送老婆 @某人`、`换妻 @某人`：过户或互换今日正缘，对方原配会被挤走。
- 新增 `保护老婆`（每日一次免疫抢婚）、`宠老婆` / `贴贴`（加契合度）、`复婚`（找回今日刚离或刚被抢走且仍空窗的前任）。
- 新增 `查户口 @某人`、`姻缘榜`（恩爱榜 / 渣男榜 / 倒霉榜）；`抽老公` 作为抽取别名。
- 抢婚失败有小概率【反噬】：你的正缘嫌你丢人，先把契约撕了。
- 以上玩法同步给 AI 沙盒方法，群指令可直接打。

## 1.0.0（2026-08-20）

NekroAgent「抽老婆」插件首个公开发布。

- 从当前 QQ 群的 `get_group_member_list` 随机抽取一位群友作为今日正缘，不抽其他群。
- 指令即时出结果：`抽老婆` / `今日老婆`、`我的老婆`、`离婚`、`本群姻缘`、`抽老婆帮助`；超管可 `重置姻缘`。
- 命运签：普通 / 稀有 / 传说 / 天命、随机羁绊称号、契合度 1–100、按稀有度换文案。
- 每人每天第一抽锁定为正缘；要换先离婚。互相抽中提示天作之合，抽到别人的正缘提示修罗场。
- 可发送 QQ 头像，文案支持 `@` 被抽中的人；可配置排除自己、机器人与黑名单 QQ。
- 自然语言场景由 AI 调用同一套抽取逻辑，并注入本群今日姻缘短摘要方便接梗。
- 仅支持 `onebot_v11` 群聊。安装时把仓库内 `choulaopo` 目录放到 `plugins/workdir/choulaopo`。
- 核心抽取与文案逻辑不依赖 NekroAgent，可单独跑 `python -m pytest tests -q`。
